”ункции:
1. Создать функцию, которая возвращает сумму двух чисел;
2. Создать функцию, которая выводит на экран сумму двух чисел;
3. Создать функцию приветствия;
4. Создать функцию, которая отрисовывает таблицу умножения;
5. ФункциЯ, принимающая массив строк и выводЯщаЯ каждую строку в отдельном К К К КК К К КК 
параграфе.


6. Создать переменную, которая будет хранить значение 25.
Взять от него синус. Результат возвести во вторую степень, используя мат. Функцию. Результат округлить
и вывести на экран.
7. Создать переменную, в которую предположительно будет помещен произвольный логин.
Вывести на экран длину строки.
Также вывести на экран первые 3 символа логина.
